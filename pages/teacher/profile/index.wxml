<!-- pages/teacher/profile/index.wxml -->
<view class="container">

  <!-- 个人信息区域 -->
  <view class="profile-card">
    <view class="profile-info" bindtap="onEditProfile">
      <image class="avatar" src="{{userInfo.avatarUrl || '/images/default-avatar.png'}}" mode="aspectFill"></image>
      <view class="profile-text">
        <text class="nick-name">{{userInfo.nickName || '教师'}}</text>
        <text class="user-code">编号：{{userInfo.userCode || ''}}</text>
      </view>
    </view>
    <view class="logout-btn" bindtap="onLogout">
      <text class="logout-icon">⏻</text>
      <text class="logout-text">退出</text>
    </view>
  </view>

  <!-- 班级管理标题 -->
  <view class="section-header">
    <text class="section-title">我的班级</text>
    <text class="section-count">{{classList.length}}个</text>
  </view>

  <!-- 班级网格 -->
  <view class="class-grid">
    <!-- 新建班级卡片 -->
    <view class="class-card add-card" bindtap="onCreateClass">
      <text class="add-icon">+</text>
      <text class="add-text">新建班级</text>
    </view>

    <!-- 已有班级卡片 -->
    <view class="class-card" wx:for="{{classList}}" wx:key="_id" bindtap="onOpenClass" data-id="{{item._id}}">
      <image class="card-bg" src="https://picsum.photos/200/150?random={{index}}" mode="aspectFill"></image>
      <view class="card-overlay">
        <text class="card-name">{{item.name}}</text>
        <text class="card-count">{{item.studentCount || 0}}名学生</text>
      </view>
    </view>
  </view>

</view>

<!-- 新建班级弹窗 -->
<view class="modal-mask" wx:if="{{showCreateModal}}" bindtap="onCloseCreateModal"></view>
<view class="modal-dialog" wx:if="{{showCreateModal}}">
  <text class="modal-title">新建班级</text>
  <input class="modal-input" placeholder="班级名称" value="{{newClassName}}" bindinput="onClassNameInput"/>
  <input class="modal-input" placeholder="邀请码（选填，留空自动生成）" value="{{newInviteCode}}" bindinput="onInviteCodeInput"/>
  <view class="modal-btns">
    <button class="modal-btn cancel" bindtap="onCloseCreateModal">取消</button>
    <button class="modal-btn confirm" bindtap="onConfirmCreate" loading="{{creating}}">确定</button>
  </view>
</view>

<!-- 修改个人信息弹窗 -->
<view class="modal-mask" wx:if="{{showEditModal}}" bindtap="onCloseEditModal"></view>
<view class="modal-dialog" wx:if="{{showEditModal}}">
  <text class="modal-title">修改信息</text>
  <input class="modal-input" placeholder="昵称" value="{{editNickName}}" bindinput="onEditNickNameInput"/>
  <input class="modal-input" type="number" maxlength="11" placeholder="手机号" value="{{editPhone}}" bindinput="onEditPhoneInput"/>
  <view class="modal-btns">
    <button class="modal-btn cancel" bindtap="onCloseEditModal">取消</button>
    <button class="modal-btn confirm" bindtap="onConfirmEdit" loading="{{saving}}">保存</button>
  </view>
</view>
