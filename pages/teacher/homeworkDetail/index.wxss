/* pages/teacher/homeworkDetail/index.wxss */
.container { min-height: 100vh; background: #f5f5f5; padding-bottom: 120rpx; }
.header { padding: 30rpx; background: #fff; margin-bottom: 20rpx; }
.hw-title { font-size: 34rpx; font-weight: bold; color: #333; display: block; margin-bottom: 10rpx; }
.hw-meta { font-size: 24rpx; color: #999; display: block; margin-bottom: 4rpx; }
.hw-deadline { font-size: 24rpx; color: #e74c3c; display: block; }
.empty { display: flex; align-items: center; justify-content: center; min-height: 40vh; }
.empty-text { font-size: 28rpx; color: #999; }
.select-bar { padding: 16rpx 30rpx; display: flex; justify-content: flex-end; }
.select-all { font-size: 26rpx; color: #667eea; }

.q-card { background: #fff; margin: 0 20rpx 16rpx; border-radius: 12rpx; padding: 20rpx; box-shadow: 0 2rpx 8rpx rgba(0,0,0,0.04); }
.q-row { display: flex; align-items: flex-start; }
.q-body { flex: 1; margin-left: 12rpx; }
.q-type {
  display: inline-block; font-size: 20rpx; padding: 2rpx 10rpx; border-radius: 6rpx;
  color: #fff; background: #5dade2; margin-bottom: 8rpx;
}
.q-type.multiple_choice { background: #f39c12; }
.q-type.fill_blank { background: #27ae60; }
.q-type.essay { background: #8e44ad; }
.q-content { font-size: 28rpx; color: #333; line-height: 1.6; }

.fix-bottom { position: fixed; bottom: 0; left: 0; right: 0; display: flex; background: #fff; padding: 16rpx 20rpx; box-shadow: 0 -2rpx 20rpx rgba(0,0,0,0.1); z-index: 100; }
.bottom-btn { flex: 1; height: 72rpx; line-height: 72rpx; font-size: 24rpx; background: #f0f0f0; border-radius: 36rpx; margin: 0 8rpx; border: none; }

.modal { position: fixed; top: 0; left: 0; right: 0; bottom: 0; z-index: 200; display: flex; align-items: flex-end; }
.modal-mask { position: absolute; top: 0; left: 0; right: 0; bottom: 0; background: rgba(0,0,0,0.5); }
.modal-content { position: relative; width: 100%; background: #fff; border-radius: 32rpx 32rpx 0 0; padding: 40rpx 30rpx; max-height: 60vh; overflow-y: auto; }
.modal-title { font-size: 34rpx; font-weight: bold; text-align: center; display: block; margin-bottom: 30rpx; }
.class-item { padding: 24rpx; background: #f9f9f9; border-radius: 12rpx; margin-bottom: 16rpx; font-size: 28rpx; }
.class-item.active { background: #e8eaff; color: #667eea; border: 2rpx solid #667eea; }
.new-group { padding: 24rpx; text-align: center; color: #667eea; font-size: 28rpx; border: 1rpx dashed #667eea; border-radius: 12rpx; margin-bottom: 16rpx; }
.modal-btns { display: flex; gap: 24rpx; margin-top: 24rpx; }
.btn-cancel, .btn-confirm { flex: 1; height: 80rpx; line-height: 80rpx; border-radius: 40rpx; font-size: 28rpx; border: none; }
.btn-cancel { background: #f0f0f0; color: #666; }
.btn-confirm { background: linear-gradient(135deg, #667eea, #764ba2); color: #fff; }
